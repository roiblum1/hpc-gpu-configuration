apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    # SR-IOV NADs are published into this namespace (sriov-rails chart). §3.2
    openshift.io/cluster-monitoring: "true"
