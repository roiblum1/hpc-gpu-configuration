apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    # The user-provided IB rail NADs are published into this namespace. §3.2
    openshift.io/cluster-monitoring: "true"
