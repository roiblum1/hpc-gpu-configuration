apiVersion: v2
name: cert-manager
description: >-
  cert-manager operator (OLM). Cross-cutting prerequisite — Dynamo operator
  webhooks (Phase 6) and Gateway TLS (Phase 7) both depend on it. Install early.
type: application
version: 0.1.0
appVersion: "1.x"
keywords: [openshift, cert-manager, olm]
